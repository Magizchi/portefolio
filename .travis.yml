language: node_js

node_js:
  - 11.6.0

cache: npm

install:
  - npm install
  - npm install -g firebase-tools

script:
  - npm run build

deploy:
  provider: firebase
  skip-cleanup: true
  local_dir: build
  token:
    secure: $FIREBASE_TOKEN
  on:
    branch: master
